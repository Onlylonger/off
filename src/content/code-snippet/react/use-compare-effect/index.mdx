---
title: 状态比较 useCompareEffect hook
description: 该 `hook` 主要是二次封装 `useEffect` 对其 deps 进行值的比较，如果值不同才会执行对应回调函数。
pubDate: "2021.3.16"
---

# {frontmatter.title}

描述：{frontmatter.description}

import Demo from "./demo/Demo";

<Demo client:load />
