---
title: 网络请求 useRequest hook
description: 异步数据请求的 `hooks`, 轻巧简单便于维护。
pubDate: "2022.6.20"
---

# {frontmatter.title}

描述：{frontmatter.description}

import Demo from "./demo/Demo";

<Demo client:load />

优点：符合`React StrictMode`，逻辑清晰简单。

缺点：内置实现了`fetch`，一起使用封装的 `fetch` 更合适。

WIP
